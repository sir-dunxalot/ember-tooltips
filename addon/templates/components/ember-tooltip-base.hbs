{{#if this._awaitingTooltipElementRendered}}
  <div>
    {{#if this._shouldRenderContent}}
      {{#if (has-block)}}
        {{yield this}}
      {{else}}
        {{this.text}}
      {{/if}}
    {{/if}}
  </div>
{{else}}
  {{#in-element this._renderElement}}
    <div>
      {{#if (has-block)}}
        {{yield this}}
      {{else}}
        {{this.text}}
      {{/if}}
    </div>
  {{/in-element}}
{{/if}}
