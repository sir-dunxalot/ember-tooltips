{{#if _shouldRender}}
	{{#component tetherComponentName
		passedPropertiesObject=passedPropertiesObject
		class=class
		id=id
	}}
    {{#if text}}
      {{text}}
    {{else}}
      {{yield (hash
        hide=(action "hide")
      )}}
    {{/if}}
	{{/component}}
{{/if}}

