{{#if _shouldRender}}
	{{#lazy-popover-on-element
		passedPropertiesObject=passedPropertiesObject
		class=class
		id=id
	}}
		{{yield (hash
			hide=(action "hide")
		)}}
	{{/lazy-popover-on-element}}
{{/if}}

